version: '2'

syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  missing-syncs-src-sync: #tip: add -sync and you keep consistent names als a convention
    dest: '/var/www'
