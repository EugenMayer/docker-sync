version: "2"

options:
  project_root: config_path

syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  project_root-sync: #tip: add -sync and you keep consistent names als a convention
    src: './app'
    dest: '/var/www'
