version: '2'

syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  missing-syncs-sync_host_port-sync: #tip: add -sync and you keep consistent names als a convention
    src: './app'
    dest: '/var/www'
    sync_strategy: 'rsync'
